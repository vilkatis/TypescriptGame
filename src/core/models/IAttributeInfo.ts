namespace Arch {
    /**
     * Represents the information needed for a GLBuffer attribute
     */
    export interface IAttributeInfo {
        location: number;
        size: number;
        offset: number;
    }
}
